┌────────────────────────────────────────────────────────────────────────────┐
│                    STGen 3-Tier Architecture Overview                      │
└────────────────────────────────────────────────────────────────────────────┘

                              ╔══════════════════╗
                              ║   TIER 3         ║
                              ║  QUERY CLIENTS   ║
                              ║ (Data Consumers) ║
                              ╚══════════════════╝
                                       │
                     ┌─────────────────┼─────────────────┐
                     │                 │                 │
              ┌──────▼──────┐   ┌─────▼──────┐   ┌─────▼──────┐
              │  Dashboard  │   │ Mobile App │   │ Analytics  │
              │             │   │            │   │   System   │
              │ Query: {}   │   │ Query:     │   │ Query:     │
              │ (all data)  │   │ node_id=A  │   │ temp>30°C  │
              └─────────────┘   └────────────┘   └────────────┘
                     │                 │                 │
                     └─────────────────┼─────────────────┘
                                       │
                              REQUEST DATA (Pull)
                                       │
                                       ▼
                              ╔══════════════════╗
                              ║   TIER 2         ║
                              ║  CORE SERVER     ║
                              ║ (Broker/Buffer)  ║
                              ╚══════════════════╝
                              │                 │
                       MQTT Broker      Data Buffer
                      (Port 1883)       (Recent data)
                              │                 │
                              ╚═════════════════╝
                                       ▲
                              PUBLISH DATA (Push)
                                       │
                     ┌─────────────────┼─────────────────┐
                     │                 │                 │
              ┌──────┴──────┐   ┌─────┴──────┐   ┌─────┴──────┐
              │   TIER 1    │   │   TIER 1   │   │   TIER 1   │
              │ SENSOR NODE │   │ SENSOR NODE│   │ SENSOR NODE│
              │     A       │   │     B      │   │     C      │
              └─────────────┘   └────────────┘   └────────────┘
              │ Location:   │   │ Location:  │   │ Location:  │
              │ Tokyo       │   │ London     │   │ Mumbai     │
              │ 2K sensors  │   │ 1.5K sens. │   │ 1K sensors │
              │ temp,humid  │   │ temp,motion│   │ temp,light │
              └─────────────┘   └────────────┘   └────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                            Data Flow Example                                │
└────────────────────────────────────────────────────────────────────────────┘

1. SENSORS PUBLISH (Continuous)
   ────────────────────────────────────────────────
   Sensor A → Core: {"node_id":"Tokyo","dev_id":"temp_001","value":25.3}
   Sensor B → Core: {"node_id":"London","dev_id":"motion_042","value":1}
   Sensor C → Core: {"node_id":"Mumbai","dev_id":"light_008","value":320}

2. CORE BUFFERS (Store recent data)
   ────────────────────────────────────────────────
   Core receives and stores messages in memory/database
   All data available for querying

3. CLIENTS REQUEST (On-demand or periodic)
   ────────────────────────────────────────────────
   Dashboard → Core: "Give me all temp sensors"
   Mobile    → Core: "Give me data from Tokyo node"
   Analytics → Core: "Give me temp above 30°C"

4. CORE RESPONDS (Filtered data)
   ────────────────────────────────────────────────
   Core → Dashboard: [25 temperature readings]
   Core → Mobile: [all Tokyo sensor data]
   Core → Analytics: [3 high temperature alerts]


┌────────────────────────────────────────────────────────────────────────────┐
│                          Real-World Example                                 │
└────────────────────────────────────────────────────────────────────────────┘

SCENARIO: Smart Building Management System

TIER 1 - Sensors (Data Publishers)
├── Floor 1: 500 sensors (HVAC, lights, occupancy)
├── Floor 2: 500 sensors (HVAC, lights, occupancy)
├── Floor 3: 300 sensors (HVAC, lights, occupancy)
└── Parking: 200 sensors (cameras, gates, lights)

TIER 2 - Core Server (Cloud/On-Premise)
├── MQTT Broker (receives all sensor data)
├── Data Buffer (last 1 hour of data)
├── Query Handler (processes client requests)
└── Database (long-term storage)

TIER 3 - Query Clients (Data Consumers)
├── Facility Dashboard: "Show me all high temps"
├── Energy Manager: "Give me HVAC power usage"
├── Security App: "Show me parking camera feeds"
├── Maintenance System: "Alert on sensor failures"
└── Analytics Platform: "Export data for ML training"


┌────────────────────────────────────────────────────────────────────────────┐
│                        Query Filter Examples                                │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────┬─────────────────────────────────────────────────┐
│ Use Case                │ Query Filter                                    │
├─────────────────────────┼─────────────────────────────────────────────────┤
│ All data from one node  │ {"node_id": "Tokyo"}                           │
│ All temperature sensors │ {"sensor_type": "temp"}                        │
│ High temp alerts        │ {"sensor_type": "temp", "min_value": 35}      │
│ Specific device         │ {"dev_id": "temp_042"}                         │
│ Multiple sensor types   │ {"sensor_type": ["temp", "humidity"]}         │
│ Node + type combo       │ {"node_id": "Floor1", "sensor_type": "motion"}│
│ Value range             │ {"sensor_type": "pressure", "min_value": 90,  │
│                         │  "max_value": 120}                             │
└─────────────────────────┴─────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────────────┐
│                      Deployment Commands                                    │
└────────────────────────────────────────────────────────────────────────────┘

EASY MODE (Interactive):
$ ./deploy_three_tier.sh
Choose: 1=Core, 2=Sensor, 3=Client

DEMO MODE (Local testing):
$ ./demo_three_tier.sh
Starts all 3 tiers automatically

MANUAL MODE:
# Core Server
$ python distributed/core_node.py --bind-ip 0.0.0.0 --protocol mqtt

# Sensor Node
$ python distributed/sensor_node.py \
    --core-ip CORE_IP --node-id Tokyo --sensors 1000

# Query Client
$ python distributed/query_client.py \
    --server-ip CORE_IP --query-filter '{"sensor_type": "temp"}'


┌────────────────────────────────────────────────────────────────────────────┐
│                         Key Features                                        │
└────────────────────────────────────────────────────────────────────────────┘

✅ Multiple sensors nodes can publish simultaneously
✅ Multiple query clients can request simultaneously
✅ Works over local network OR public internet
✅ Filter by: node, sensor type, device, value range
✅ Query modes: one-time OR continuous monitoring
✅ Export results to JSON for analysis
✅ Real-time data access
✅ Scalable architecture


┌────────────────────────────────────────────────────────────────────────────┐
│                        Documentation Links                                  │
└────────────────────────────────────────────────────────────────────────────┘

📖 README_THREE_TIER.md          - Quick reference (you are here)
📖 THREE_TIER_ARCHITECTURE.md    - Complete architecture guide
📖 PUBLIC_NETWORK_DEPLOYMENT.md  - Public internet deployment
🛠️  deploy_three_tier.sh          - Interactive deployment
🎬 demo_three_tier.sh             - Local demo


YES, YOUR SYSTEM FULLY SUPPORTS CLIENT REQUESTS! 🎉
